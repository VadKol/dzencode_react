import { FC } from "react";

export const Header: FC = () => {
  return (
    <header>
      Logo
      <input
        type="text"
        placeholder="Search product"
      />
      TopMenu
    </header>
  );
};